# ConvNet for CIFAR10 in PyTorch

## Goals
* Learn to use PyTorch
* Achieve at least 70% accuracy on test set
** Got 82% in val set on custom convnet
* Use imported ResNet
* Build own ResNet architecture
